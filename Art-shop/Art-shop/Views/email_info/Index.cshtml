@model IEnumerable<Art_shop.Models.email_info>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage_admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.email_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.email_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.email_id })
        </td>
    </tr>
}

</table>
